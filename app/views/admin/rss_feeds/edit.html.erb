<div class="box">
  <div class="legend">Edit RSS Feed</div>
  <%= render 'form' %>

  <hr style="margin: 2rem 0;">

  <h3>Danger Zone</h3>
  <%= button_to "Delete Feed", admin_rss_feed_path(@feed),
      method: :delete,
      data: { confirm: "Are you sure you want to delete this feed? All fetched items will also be deleted." },
      style: "background: #d9534f; color: white; border: none; padding: 0.5rem 1rem; cursor: pointer;" %>
</div>
